export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  ROLE = 'role',
  IMAGE = 'image',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  ROLE = 'ROLE_SERVICE',
  IMAGE = 'IMAGE_SERVICE',
}
